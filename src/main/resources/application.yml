spring:
  datasource:
    url: jdbc:mariadb://svc.sel5.cloudtype.app:30745/test
    driver-class-name: org.mariadb.jdbc.Driver
    username: test_user
    password: test1234

  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: none

  mvc:
    pathmatch:
      matching-strategy: "ant_path_matcher"
